# Основные зависимости для сервера инференса PyTorch модели верификации подписей

# === WEB FRAMEWORK ===
fastapi>=0.104.0
uvicorn>=0.24.0  # Без [standard] для уменьшения размера (не нужны httptools, uvloop, watchfiles для serverless)
python-multipart>=0.0.6

# === ML & DL ===
# CPU-only PyTorch (wheel index: https://download.pytorch.org/whl/cpu).
# 2.7.x снят с индекса; для Python 3.12–3.14 доступны 2.9+ (локально проверено на 3.14).
# torchvision не используется в inference.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
numpy>=1.26,<3  # PyTorch 2.x совместим с numpy 2.x
scipy>=1.11
pillow>=10.0

# === DATABASE ===
supabase>=2.5

# === UTILITIES ===
python-dotenv>=1.0
psutil>=5.9.0

# === DEVELOPMENT ===
# Удалите эти зависимости для продакшена или используйте requirements-dev.txt
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# httpx>=0.24.0
# black>=23.0.0
# flake8>=6.0.0

# === PRODUCTION ===
# gunicorn удален - не нужен для Vercel serverless functions
mangum>=0.17.0  # ASGI adapter for Vercel serverless functions
